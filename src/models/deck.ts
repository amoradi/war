import Card from "./card";

class Deck {
  deal() {
    // cosnt hands = this.cut();
    // return 2 Hands of 26
    // return [new Hand(hands[0]), new Hand(hands[1])];
  }

  shuffle() {
    // randomize items in the deck
  }

  private cut() {
    // return an array of len 2
    //
  }
}
